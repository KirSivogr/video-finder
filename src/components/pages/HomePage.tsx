import { Header } from '@components/header/Header';
import { HomePageContent } from '@components/page-content/home-page-content/HomePageContent';

export const HomePage = () => {
   return (
      <>
         <Header />
         <HomePageContent />
      </>
   );
};
